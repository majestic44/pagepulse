{
  "name": "@pagepulse/control-worker",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "lint": "eslint src",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@pagepulse/config": "workspace:*",
    "@pagepulse/contracts": "workspace:*",
    "bullmq": "^5.66.4",
    "ioredis": "^5.8.2"
  },
  "devDependencies": {
    "tsx": "^4.20.6"
  }
}
